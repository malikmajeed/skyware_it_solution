# Contact form – used when building the static site (no-Node hosting).
# The form POSTs directly to this URL. Set it to where send-contact.php is deployed.
NEXT_PUBLIC_CONTACT_FORM_URL=https://yourdomain.com/send-contact.php
